# When running `npm run dev`, Vite proxies /api/* to the backend on port 8000.
# So for development, leave this empty or unset.
# For production (built app), set this to the backend API URL, e.g.:
# VITE_API_BASE_URL=https://your-ngrok-url.ngrok-free.app
VITE_API_BASE_URL=
